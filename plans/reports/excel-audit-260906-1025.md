# Excel Parsing Architecture Audit — 2026-09-06

Evidence base for `plans/260906-1120-excel-parsing-refactor/`. Three read-only
exploration passes (excel pkg, satellite islands, transport/workers/docs) + direct
source verification. All paths relative to `backend/` unless noted.

## Routing (BCC family) — three stacked layers
1. `DetectFormat` (`internal/app/services/excel/format_detector.go:51`) — priority ladder Legacy > WeeklyBCC > WeeklyPayment > MultiPosition > DateRow; fingerprints interleaved in one loop; date-row cell-scan gated `:104`.
2. Dispatch switch (`internal/app/services/bcc_import_process.go:69-92`) → per-format `process*Upload`; **winner-format contract**: `formatResult.Format = winner` `:90` feeds `:162` (STK-from-employees merge), `:319` (file-position precedence), `:381` (`labelKeyedFile = ratelessFile && FormatDateRow`).
3. `ParseBCCData` registry (`excel/bcc_strategy.go:38`, legacy+dateRow, Match/Parse/Accept/Format) — reachable only via default branch; fingerprints duplicate Layer 1; comment `:14` falsely claims extension-point status.

New template today = 4–6 files (example Samsung SDS: `stk_parser.go:53`, `bcc_parser.go:213`, `bcc_import_process.go:129`, `bcc_import_helpers.go:253,367`, `samsung_sds_test.go`).

## God files
`bcc_import_weekly.go` 1,324 LOC (2 funcs: 684 + 642 lines); `bcc_import_multi_position.go:24` 482-line func; `bcc_import_process.go` 609; `settlement/onepay_fee_import_service.go` 829; `excel/timesheet_template_service.go` 795. `BCCImportService` (`bcc_import_service.go:24`) = 12-dependency orchestrator. fail()/updateAssetMetadata boilerplate ×4.

## Duplication clusters
- In excel pkg: 7 header-map builders; 4× rowHasPositiveHours; 4× raw-numeric read (only `dateRowNumericCell` factored); 5× "Tổng cộng" break; 4× "Tổng" stop-column; `parseForMonth` across packages (`services/bcc_import_helpers.go:38` vs `excel/weekly_payment_parser.go:395`); dead `buildWeekdayToDayMap` (`weekly_payment_parser.go:429`); shared helpers stranded (`bccCell` `bcc_parser.go:461`, `parseExcelDate` `weekly_bcc_parser.go:278`, `normHeader` `bcc_parser.go:130` NFC-only).
- Across islands: name normalization ×4 DIVERGENT (space-strip `onepay:742` vs space-keep `domain/ports/infrastructure/disbursement.go:259` — bank-recon keys, do NOT merge); header normalization ×3; money parsing ×5; date parsing ×4 (`bulktransfer/row_parser.go:86,91` on `time.Local` — clock-rule violation); getOrCreate trio ~180 LOC ×2 (`employee/import_service.go:269,300,433` ↔ `admin_flexpay_import.go:398,426,504`); `clearExistingDataMBank` ×2 divergent; settlement `"type:"` contract split payroll↔settlement.

## Detection idioms (satellites)
Declarative alias tables (BEST): `onepay_fee_import_service.go:71-107` + `wallet_bulk/parser.go:34`. Hardcoded sheet names: `settlement/excel_parser.go:17-30`. Blind offsets: `employee/import_service.go:56`, `admin_bulk_transfer.go:93`. Offset sniff: `admin_flexpay_import.go:50`.

## Error/progress regimes
4 error regimes (structured `OnePayFeeReportIssue` / `RowError` / sentinels / silent-skip+counters in flexpay + ProcessBankResult). 5 progress models; BCC `error_detail` JSON in Asset.Metadata = ground truth, parsed by `frontend/src/utils/import-errors.ts:86` (shape frozen).

## Upload exposure
Size caps missing at 5 endpoints (flexpay unbounded `io.ReadAll` `advance_payment/file_handler.go:205`, settlement ×2, OnePay fee, template import); zip-bomb/row caps only `wallet_bulk/parser.go:19-29` (gold standard handler: `wallet_bulk_transfer_handler.go:51-289`).

## Tests
Excel pkg: synthetic fixtures per parser; real files: eva06.xlsx, thai_binh_duong.xlsx (skip-if-absent), bcc-zero/bumhan. No dispatch-level test, no unknown-template test, no multi_position parser test. Employee/flexpay-workbook/ProcessBankResult/reporting/db_export: zero tests. Only env-gated real-file test: `ONEPAY_FEE_REAL_FILE` (`onepay_fee_import_service_test.go:194`). User staged all-5-format real corpus at `testplan/excelfixture/` (2026-09-06).

## Dead/vestigial + docs
`bccinspect` legacy-only (lies on date-row); `scripts/diagnose_bcc.go:13` hardcoded path; `includeFlexibleEmployees` double-dead (403+400); `EnqueueJob` no-op; metadata aliases; vestigial `ResultStrategyFactory`. No `AGENTS.md` in services/excel; stale `docs/codebase-summary.md:143`, `frontend/src/components/timesheet/AGENTS.md:76`.
