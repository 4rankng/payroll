# Fix All TypeScript Errors — Parallel Agent Plan

**Total errors: 194 across 85 files** (from `npx tsc -p tsconfig.app.json --noEmit`)
**Working directory:** `frontend/`
**Verify command:** `npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep "error TS" | wc -l`

---

## Agent 1: Type Definition Fixes (ROOT CAUSE — run first)

**Focus:** Fix shared type definitions that cascade into many consumer errors.

### Files to edit:
- `src/types/api/timesheet.types.ts`
- `src/types/api/loan.types.ts`
- `src/types/api/employee.types.ts`
- `src/types/api/payrate.types.ts`

### Tasks:
1. **`EmployeeTimesheetEntry` (timesheet.types.ts:271)** — Missing `created_by`, `approved_by`. Add:
   - `created_by: number;`
   - `approved_by: number | null;`

2. **`Timesheet` interface (timesheet.types.ts:6)** — `status` field is `'approved' | 'pending_approval' | 'rejected' | 'draft'` but `TimesheetStatus` type includes `'paid' | 'failed' | 'cancelled' | 'pending'`. No change needed to interface itself; the issue is consumers using wrong property names.

3. **`TimesheetStatus` (timesheet.types.ts:481)** — Currently has all 8 values. This is fine. The `AggregatedStatus` type in `timesheetStatusColors.ts` needs to include `'none'`.

4. **`Loan` interface (loan.types.ts:89)** — Missing `disbursed_at` and `start_date`. Add:
   - `disbursed_at?: string;`
   - `start_date?: string;`

5. **`ScheduleGenerationResult`** — This is a local type in `src/hooks/useLoanCalculations.ts:51`. It's defined as `{ success: true } | { success: false; error: string; fieldErrors?: FieldErrors }`. The consumer in `AddLoanSheet.tsx` accesses `.error` and `.fieldErrors` without first checking `success === false`. Fix: use type narrowing in consumer.

### Errors resolved: ~15 direct + cascading

---

## Agent 2: Property Name Fixes (camelCase ↔ snake_case)

**Focus:** Fix all TS2551 "Did you mean..." and property name mismatches.

### Files & changes:
1. **`src/components/timesheet/components/useTimesheetEntryForm.ts:267-268`** — `employeeId` → `employee_id`, `projectId` → `project_id`
2. **`src/hooks/api/useTimesheets.ts:250-251`** — Same: `employeeId` → `employee_id`, `projectId` → `project_id`
3. **`src/pages/admin/TimesheetPage/index.tsx:178-179`** — Same: `projectId` → `project_id`, `employeeId` → `employee_id`
4. **`src/pages/mobile/admin/TimesheetPage/index.tsx:223-224`** — Same
5. **`src/hooks/timesheet/useTimesheetEntry.ts:86-87`** — `from_date` → `fromDate`, `to_date` → `toDate` (PayRate uses camelCase)
6. **`src/components/ui/user-card-row.tsx:154,163`** — `lastLogin` → `last_login`, `createdAt` → `created_at`
7. **`src/hooks/timesheet/useTimesheetData.ts:41`** — `timesheetIds` → `timesheet_ids` (in BulkApproveData)
8. **`src/utils/timesheet.utils.ts:63-68`** — `calculatedAmount` → `calculated_amount`, `hoursWorked` → `hours_worked`, `hourType` → `hour_type`, `calculatedRate` → `calculated_rate` (or check actual TimesheetEntry interface and adjust)
9. **`src/utils/timesheetHelpers.ts:39`** — Compare against English status values not Vietnamese `"Chờ duyệt"`
10. **`src/hooks/timesheet/useTimesheetFilters.ts:23`** — `description` doesn't exist on `Timesheet`. Remove or use `notes`.
11. **`src/components/timesheet/components/useTimesheetEntryForm.ts:267-268`** — Already covered above.

### Errors resolved: ~15

---

## Agent 3: API Response Unwrapping Fixes

**Focus:** Fix `ApiResponse<T>` vs `T` mismatches across services and hooks.

### Key insight:
`apiClient.get<T>()` returns `ApiResponse<T>`, which has `{ status, data, message }`. Many callers forget to unwrap `.data`.

### Files & changes:
1. **`src/services/api/advance-payment.service.ts:284,288`** — `ApiResponse` used with 2 type args. Remove the second or use the correct generic.
2. **`src/services/api/auth.service.ts:79,110`** — `ApiResponse<unknown>` assigned to `ApiResponse<void>`. Cast or use `as ApiResponse<void>`.
3. **`src/services/api/ledger.service.ts:166`** — Response wrapper not unwrapped. Extract `.data` from the wrapped response.
4. **`src/services/api/ledger.service.ts:273`** — Same, unwrap response.
5. **`src/services/api/payroll.service.ts:23`** — Casting `ApiResponse<PaymentHistoryListResponse>` to `PaymentHistoryListResponse`. Use `.data` instead.
6. **`src/services/api/transaction.service.ts:283`** — Same unwrapping issue.
7. **`src/hooks/partner/useDeletePartnerProject.ts:13`** — `ApiResponse<void>` assigned to `void`. Don't return the value.
8. **`src/hooks/partner/usePartnerProject.ts:21`** — Accessing `.success` on `ApiResponse<ProjectEmployee[]>`. Use `.status === 'success'` or check `data`.
9. **`src/hooks/partner/useUpdatePartnerProject.ts:19,66`** — `ApiResponse<Project>` assigned to `Project`. Unwrap with `.data`.
10. **`src/hooks/partner/useUpdatePartnerProject.ts:46,94`** — `Project` not assignable to `Record<string, unknown>`. Use spread `...project`.
11. **`src/hooks/usePushNotifications.ts:93`** — `.publicKey` on `ApiResponse<{publicKey: string}>`. Use `.data.publicKey`.
12. **`src/hooks/transactions/useCreateTransaction.ts:15-16`** — `.message` not on `TransactionWithLedgerResponse`. Check if it's wrapped in ApiResponse.
13. **`src/components/modals/SettleTransactionModal.tsx:158`** — `.id` on `ApiResponse<Asset>`. Use `.data.id`.

### Errors resolved: ~25

---

## Agent 4: Component Prop & UI Type Fixes

**Focus:** Fix component prop mismatches, React type issues, and UI library type fixes.

### Files & changes:
1. **`src/components/advance-payment/AdvPartnerStatusOverview.tsx:142,185,186`** — Missing `successRate` in props. Add `successRate: 0` or calculate it.
2. **`src/components/notifications/NotificationProvider.tsx:42,45,48`** — Remove 3 unused `@ts-expect-error` directives.
3. **`src/components/partner-employees/mobile/PartnerEmployeesListMobile.tsx:68,85,98`** — `unknown` not assignable to `ReactNode`. Cast with `as ReactNode` or use type narrowing.
4. **`src/components/payrates/types.ts:364`** — `unknown` to `number`. Add type assertion or proper typing.
5. **`src/components/payroll/PaymentHistorySheet.tsx:69`** — `const` assertion on non-literal. Remove `as const`.
6. **`src/components/project-employees/AddEmployeesToProject.tsx:61,71,247,260,273,290,353`** — Multiple issues: const assertion, sort order type, `minWidth` not in ColumnDef, missing `pageSize` prop. Fix type annotations.
7. **`src/components/timesheet/EditRequestTable.tsx:178`** — Remove `as const`.
8. **`src/components/timesheet/mobile/HourTypeSelector.tsx:57`** — `IconComponent` typed as `unknown`. Add proper LucideIcon typing.
9. **`src/components/timesheet/TimesheetContext.tsx:102`** — `projectEmployees` missing `name` property. Map to include `name` field.
10. **`src/components/timesheet/TimesheetFilters.tsx:134,193`** — Employee[] type mismatch. Fix the mapping.
11. **`src/components/transaction/SettlementHistorySection.tsx:92`** — `Record<number, User>` to `Map<number, User>`. Convert to Map.
12. **`src/components/ui/accordion.tsx:30`** — `unknown` to `LegacyRef<HTMLDivElement>`. Type the ref properly.
13. **`src/components/ui/bank-selector.tsx:173`** — `TriggerProps.value` type mismatch. Fix the value type.
14. **`src/components/ui/data-table.tsx:79,82`** — `accessorKey` on `unknown`. Properly type column definitions.
15. **`src/components/ui/employee-selector.tsx:61,91`** — Status type mismatches.
16. **`src/components/ui/FileList.tsx:146,149,164`** — Missing `status` and `is_public` on Asset type.
17. **`src/components/ui/mobile-table.tsx:172,228`** — `T[keyof T]` not ReactNode. Cast appropriately.
18. **`src/components/ui/searchable-dropdown.tsx:77-120`** — Missing `subtitle` in option type.
19. **`src/components/ui/sheet.tsx:57`** — Missing `description` in `SheetContentProps`.
20. **`src/components/ui/sonner.tsx:63`** — `unknown` to `titleT`. Type assertion.
21. **`src/components/ui/typography.tsx:36-37`** — SVG ref type mismatch. Use correct ref type.
22. **`src/components/wallet/WalletTransactionsList.tsx:249`** — Missing `description` prop on SheetContent. Same as sheet.tsx issue.

### Errors resolved: ~50

---

## Agent 5: Hooks Type Fixes

**Focus:** Fix type errors in all hooks (excluding those covered by Agents 2 and 3).

### Files & changes:
1. **`src/hooks/api/useAdvancePayments.ts:384`** — `unknown` to `number`. Type assertion.
2. **`src/hooks/api/useAssets.ts:28`** — `AssetFilters` has no common properties. Fix the import/type.
3. **`src/hooks/api/useAttendance.ts:27,31,42,48`** — `toast` called incorrectly (`.success`/`.error` on wrong type). Use `toast.success()` pattern.
4. **`src/hooks/api/useEmails.ts:24`** — `unknown` to `number`. Type assertion.
5. **`src/hooks/api/useEmployees.ts:152`** — `status` type mismatch. `AssignmentStatus` doesn't include `'ended'`.
6. **`src/hooks/api/useEmployees.ts:337`** — Expected 1 arg, got 2. Fix function call.
7. **`src/hooks/business/useProjectEmployeeManagement.ts:43`** — Remove `as const`.
8. **`src/hooks/employees/useEmployeeData.ts:187`** — Return type mismatch. Fix function signature.
9. **`src/hooks/employees/useEmployeeDetails.ts:37`** — Remove `as const`.
10. **`src/hooks/employees/useEmployeeExport.ts:15`** — `{ projectIds: string }` not `EmployeeFilters`. Fix the filter.
11. **`src/hooks/ledger/useLedgerMetadata.ts:16`** — `cacheTime` doesn't exist. Use `gcTime` (TanStack Query v5).
12. **`src/hooks/ledger/useLedgerStatsConfig.ts:26,66,78`** — `NoInfer` type issues and `.net` vs `.net_amount`. Fix data access.
13. **`src/hooks/partner-timesheet/usePartnerTimesheetData.ts:44,45,104,105`** — `never` type. Fix query generic types.
14. **`src/hooks/projects/useProjectFilters.ts:15`** — Impossible comparison with `'all'`. Fix logic.
15. **`src/hooks/timesheet/useTimesheetActions.ts:41,57`** — Wrong argument types for service calls.
16. **`src/hooks/timesheet/useTimesheetManagement.ts:72`** — Remove `as const`.
17. **`src/hooks/useModalManager.ts:48`** — Index signature mismatch. Type assertion.
18. **`src/hooks/useModalNavigation.ts:277`** — Missing `SATURDAY_DAY_TYPE`. Add to the constant object.
19. **`src/hooks/usePushNotifications.ts:132`** — `Uint8Array` not assignable to `BufferSource`. Convert with `new Uint8Array(...)` as any.
20. **`src/hooks/useSenderProfiles.ts:45`** — `.forEach` on `Record`. Use `Object.entries()`.

### Errors resolved: ~30

---

## Agent 6: Pages & Misc Fixes

**Focus:** Fix remaining errors in pages, services, utils, and lib files.

### Files & changes:
1. **`src/pages/admin/AdvancePaymentsPage/index.tsx:420,423`** — `limit` → `pageSize`, title/description not ReactNode.
2. **`src/pages/admin/AuditLogPage/index.tsx:40`** — fetchNextPage type mismatch. Wrap in async arrow.
3. **`src/pages/admin/LedgerEntriesPage/index.tsx:239`** — `.length` on `NoInfer`. Type assertion.
4. **`src/pages/admin/ProjectsPage/index.tsx:40`** — Can't set `"active"` when state type is union with array. Fix state type.
5. **`src/pages/admin/TransactionsPage/index.tsx:97`** — String to `"desc"|"asc"`. Cast.
6. **`src/pages/admin/UsersPage/index.tsx:40`** — Unknown `data` property. Fix API call.
7. **`src/pages/Login.tsx:152-155`** — `Error` to `ApiError` cast. Use `as unknown as ApiError`.
8. **`src/pages/mobile/admin/AuditLogPage/index.tsx:28`** — Same fetchNextPage issue.
9. **`src/pages/mobile/admin/EmployeesPage/index.tsx:202`** — Expected 1 arg, got 0. Pass required arg.
10. **`src/pages/mobile/admin/LedgerEntriesPage/index.tsx:241`** — `.length` on NoInfer. Same pattern.
11. **`src/pages/mobile/admin/ProjectsPage/index.tsx:63`** — Same as desktop ProjectsPage.
12. **`src/pages/mobile/admin/TimesheetPage/index.tsx:416`** — Missing `onProjectBulkApprove` prop. Add it.
13. **`src/pages/mobile/partner/TimesheetsPage/index.tsx:254`** — Missing `viewMode` prop. Add it.
14. **`src/pages/mobile/partner/TimesheetsPage/PaymentHistoryPage.tsx:53,157`** — Remove `as const`, fix fetchNextPage type.
15. **`src/pages/partner/EmployeesPage/index.tsx:92`** — Missing `openTimesheetEntry`. Add to actions.
16. **`src/lib/cache/invalidationService.ts:139-170`** — `.push` on `readonly unknown[][]`. Change to mutable array or use spread.
17. **`src/lib/cache/queryPersister.ts:77`** — `filter` not in `PersistQueryClientOptions`. Remove or use correct API.
18. **`src/lib/queryKeys/index.ts:374`** — `.all` not on union type. Narrow the type first.
19. **`src/lib/realtime.ts:182,206,286`** — `unknown` to enum type. Type assertions.
20. **`src/components/sheets/AdvancePaymentSheet.tsx:149`** — Missing `forMonth` in request.
21. **`src/components/sheets/timesheet-entry/hooks/useTimesheetFormState.ts:110,144`** — Missing `current_projects` in Employee. Add it.
22. **`src/components/system-health/ErrorByEndpointTable.tsx:21`** — String to number arg. Parse or fix type.
23. **`src/components/timesheet/BulkTransferExportDialog.tsx:221`** — Employee[] mismatch. Map with proper shape.
24. **`src/components/timesheet/ChuyenLoDialog/index.tsx:570`** — Same Employee[] issue.
25. **`src/components/timesheet/components/CalendarDayCell.tsx:68,216`** — `'none'` not in AggregatedStatus. Add `'none'` to the type.
26. **`src/components/timesheet/utils/timesheetCalendarHelpers.ts:316,320,325-327`** — Missing properties on EmployeeTimesheetEntry. Add `timesheet_status`, `project`, `created_by`.
27. **`src/components/timesheet/utils/timesheetStatusColors.ts:45,81,121,140`** — Missing status values in Records. Add entries for all enum values.
28. **`src/utils/avatarHelpers.ts:19`** — `clothingColor` not in options. Use correct property name.
29. **`src/utils/cacheUpdates.ts:173,242,249,304`** — Type assertions needed for generics.
30. **`src/utils/dom-safety.ts:101`** — `webkitTemporaryStorage` not on Navigator. Type assertion.
31. **`src/utils/employeePortal/timesheetGrouping.ts:60`** — Missing `paymentStatus`. Add it.
32. **`src/utils/employees/timesheetHelpers.ts:26`** — `project_id` on `never`. Fix type narrowing.
33. **`src/utils/excel/bulk-transfer-history-export.ts:89`** — Index signature missing. Cast.
34. **`src/utils/loanHelpers.ts:194`** — Missing `disbursed_at`, `start_date` on Loan. (Fixed by Agent 1 type def.)
35. **`src/utils/payrate-test.ts:103`** — Missing `fromDate`, `toDate`, `created_by`. Add to test data.
36. **`src/utils/pdf/pdfmake.ts:4,74,111,117`** — `PdfMakeWithVfs` extends `PdfMake`. Fix interface or use type assertion.
37. **`src/utils/scheduleSummary.ts:25`** — Impossible comparison. Fix the type/value.
38. **`src/components/ledger/LedgerEntryDetailsSheet.tsx:144`** — `.find` on NoInfer. Type assertion.
39. **`src/components/sheets/AddLoanSheet.tsx:122-216`** — `.error`/`.fieldErrors` on success result. Add type narrowing.

### Errors resolved: ~60

---

## Execution Order

```
Agent 1 (type defs) → must complete first (cascading fixes)
Agents 2-6 → can run in parallel after Agent 1
Final: run `npx tsc -p tsconfig.app.json --noEmit` to verify 0 errors
```

## Verification

After all agents complete:
```bash
cd frontend && npx tsc -p tsconfig.app.json --noEmit 2>&1 | grep "error TS" | wc -l
# Target: 0
```
