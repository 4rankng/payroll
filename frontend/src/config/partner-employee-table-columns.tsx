import { ColumnDef } from "@tanstack/react-table";
import { Badge } from "@/components/ui/badge";
import { User, Calendar, CheckCircle } from "lucide-react";
import { ProjectEmployee } from "@/types/partnerProject";
import { getEmployeeStatusColor } from "@/utils/partnerProjectHelpers"; export const createPartnerEmployeeColumns = (): ColumnDef<ProjectEmployee>[] => [ { accessorKey: "name", header: ({ column }) => ( <div className="text-left typography-label-medium">Họ tên</div> ), cell: ({ row }) => ( <div className="flex items-center gap-3"> <div className="w-8 h-8 bg-primary/10 rounded-full flex items-center justify-center"> <User className="w-4 h-4 text-primary" /> </div> <div> <p className="typography-label-medium">{row.getValue("name")}</p> <p className="typography-body-medium text-muted-foreground">{row.original.position}</p> </div> </div> ), }, { accessorKey: "department", header: ({ column }) => ( <div className="text-left typography-label-medium">Phòng ban</div> ), cell: ({ row }) => ( <div className="flex items-center gap-2"> <div className="w-2 h-2 bg-purple-500 rounded-full"></div> <span className="typography-body-medium">{row.getValue("department")}</span> </div> ), }, { accessorKey: "joinDate", header: ({ column }) => ( <div className="text-left typography-label-medium">Ngày tham gia</div> ), cell: ({ row }) => ( <div className="flex items-center gap-2"> <Calendar className="h-4 w-4 text-muted-foreground" /> <span className="typography-body-medium">{row.getValue("joinDate")}</span> </div> ), }, { accessorKey: "status", header: ({ column }) => ( <div className="text-left typography-label-medium">Trạng thái</div> ), cell: ({ row }) => ( <Badge className={getEmployeeStatusColor(row.getValue("status"))}> <CheckCircle className="w-3 h-3 mr-1" /> {row.getValue("status")} </Badge> ), },
];